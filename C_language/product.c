#include <stdio.h.>

int a, b, product;

int main() {

    //Data input
    printf("enter two numbers:");
    scanf("%d %d", &a, &b);

    //Multiply formulae
    product = a * b;

    //text display
    printf("product of two numbers is: %d", product);
    
    return 0;
}