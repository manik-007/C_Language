//program to print area of square

# include <stdio.h>

int main() {
    
    float side;
    printf("Enter side", side);
    scanf("%f", &side);
    // int area = side * side;

    printf("area is : %f", side * side);
    return 0;
}